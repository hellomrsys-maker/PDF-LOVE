{
  "name": "pdflove-desktop",
  "version": "0.4.0",
  "private": true,
  "description": "Desktop shell for PDF Love. The UI is ../frontend, unmodified.",
  "scripts": {
    "web": "node ../build.mjs",
    "engine": "python3 build-engine.py",
    "tauri": "tauri",
    "dev": "npm run web && npm run engine && tauri dev",
    "build": "npm run web && npm run engine && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}

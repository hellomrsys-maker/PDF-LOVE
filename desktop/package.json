{
  "name": "dockbench-desktop",
  "version": "0.4.0",
  "private": true,
  "description": "Desktop shell for Dockbench. The UI is ../frontend, unmodified.",
  "scripts": {
    "engine": "python3 build-engine.py",
    "tauri": "tauri",
    "dev": "npm run engine && tauri dev",
    "build": "npm run engine && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
